build/
build-surge-plugin/
build-san/
build-mingw/
cmake-build-*/
.cache/
# Local Linux->Windows cross-compile experiment scratch (vcpkg mingw/clang-cl, xwin SDK)
.xwin-cache/
*.o
*.a

# Built Surge XT LV2 bundle, staged by scripts/build-surge-plugin.sh and packaged in
# releases (built in CI). A 37 MB binary — not tracked; regenerate with the script.
third_party/surge-plugin/

# Python bytecode (tools/)
__pycache__/

# Manual (Material for MkDocs). Generated reference output is rebuilt in CI from
# the source of truth (proto comments, osc.yaml, docstrings) — never committed.
site/
.cache/plugin/
docs/control-scripting/reference/grpc/*
!docs/control-scripting/reference/grpc/index.md
docs/control-scripting/reference/osc/*
!docs/control-scripting/reference/osc/index.md
docs/control-scripting/reference/lisp/*
!docs/control-scripting/reference/lisp/index.md

# Common Lisp: ocicl-managed vendored dependency systems. Reinstall with
# `ocicl install` from common-lisp/ocicl.csv (the lockfile IS tracked).
common-lisp/ocicl/

# Python packaging build artifacts
*.egg-info/
licenses/

# Generated package output (rpmbuild -bb / local package builds)
rpmbuild/
/out/
